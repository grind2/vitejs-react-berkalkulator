import InputDiscounted from './InputDiscounted'

export default function FamilyDiscount({d, sd, nd, snd}) {
    
    return (

      <InputDiscounted d={d} sd={sd} nd={nd} snd={snd}></InputDiscounted>
    )
  
}
